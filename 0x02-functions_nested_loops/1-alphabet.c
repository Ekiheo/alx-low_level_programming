#include <stdio.h>

/**
 * main - prints alphabest in lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
		putchar("%c", a);

	putchar("\n");

	return (0);
}
