#include <main.h>

/**
 * main - Prints _pritchar
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char *s_ = "_putchar"

	#include "main.h"

		while (*s_)
		{
			_putchar(*s_);
			*s_++;
		}
	_putchar('\n');

	return (0);
}
